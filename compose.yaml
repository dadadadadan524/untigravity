version: "3.9"

services:
  web:
    build: .
    container_name: untigravity-web
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
